<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:itemTextAppearance">@style/AppTheme.ItemTextStyle</item>
        <item name="android:dateTextAppearance">@style/AppTheme.dateTextAppearance</item>
        <item name="android:weekDayTextAppearance">@style/AppTheme.weekDayTextAppearance</item>
    </style>

    <!-- Options Menu style -->
    <style name="AppTheme.ItemTextStyle" parent="@android:style/TextAppearance.Widget.IconMenu.Item">
        <item name="android:textColor">@color/colorLightGreen</item>
        <item name="android:textSize">45dp</item>
    </style>


    <!-- Calendar date style -->
    <style name="AppTheme.dateTextAppearance" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:color">@color/colorPrimary</item>
    </style>

    <!-- Calendar week day style -->
    <style name="AppTheme.weekDayTextAppearance" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="LoginProgressDialog" parent="Theme.AppCompat.Dialog">

        <item name="android:windowBackground">@color/colorInvisible</item>
        <item name="android:textColor">@color/colorLightGreen</item>
        <item name="android:textColorPrimary">@color/colorDarkGreen</item>
        <item name="colorAccent">@color/colorDarkGreen</item>
    </style>

</resources>